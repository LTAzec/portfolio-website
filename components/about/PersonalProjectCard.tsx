// Deprecated.
//
// The About page's Personal Projects section now uses <ProjectShelf />
// which pulls projects directly from `data/projects.ts` — the single
// source of truth — so adding a project to projects.ts is the only
// step required for it to show up on /about.
//
// This file is intentionally empty (kept because `rm` is not permitted
// in this workspace mount). It can be safely deleted manually.
export {};
